# Pinned to the versions this code was tested against. Bump deliberately,
# then re-run the test suite (pytest) before deploying.
google-genai==2.3.0
apscheduler==3.11.2
line-bot-sdk==3.23.0
myanmartools==1.2.1
python-myanmar==1.10.0
playwright==1.59.0
python-dotenv==1.2.2
requests==2.34.2
flask==3.1.3
waitress==3.0.2
